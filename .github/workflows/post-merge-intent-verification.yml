name: Post-Merge Maintainer Body Apply (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired compatibility marker
        run: echo "Retired by issue #2469; no automatic trigger, PR mutation, issue mutation, or closeout ownership remains."

name: GATE — Branch Freshness

# Manual-only: rebuild as advisory gate in a follow-up issue. Do not restore as auto-trigger marker.

on:
  workflow_dispatch: {}

jobs:
  branch-freshness:
    runs-on: ubuntu-latest
    steps:
      - name: Manual-only marker
        run: echo "Branch freshness is manual-only pending rebuild; not a merge blocker."

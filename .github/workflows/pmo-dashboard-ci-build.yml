name: PMO dashboard CI build (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: PMO dashboard CI disabled
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "PMO dashboard CI build is disabled pending Operations redesign."

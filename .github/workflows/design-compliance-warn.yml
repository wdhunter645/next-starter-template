name: Design Compliance (Warn)

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  design_compliance_warn:
    runs-on: ubuntu-latest
    steps:
      - name: Transition note
        run: echo "Design compliance warning is manual during PR process redesign."

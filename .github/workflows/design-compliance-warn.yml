name: Design Compliance (Warn)

# Manual-only: rebuild as advisory gate in a follow-up issue. Do not restore as auto-trigger marker.

on:
  workflow_dispatch: {}

jobs:
  design_compliance_warn:
    runs-on: ubuntu-latest
    steps:
      - name: Manual-only marker
        run: echo "Design compliance warn is manual-only pending rebuild; not a merge blocker."

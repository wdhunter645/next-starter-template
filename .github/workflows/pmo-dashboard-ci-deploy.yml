name: PMO dashboard CI deploy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: PMO dashboard deploy disabled
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "PMO dashboard CI deploy is disabled pending Operations redesign."

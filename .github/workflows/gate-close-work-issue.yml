name: gate-close-work-issue

on:
  pull_request_target:
    types: [closed]

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated — parked no-op issue close gate
        run: |
          echo "Deprecated: this workflow is intentionally parked and performs no issue mutation."
          echo "Post-merge source issue closeout is owned by .github/workflows/post-merge-closeout.yml."
          echo "Pre-merge issue accounting is owned by .github/workflows/ops-pr-issue-accounting.yml."
          echo "See docs/reference/ci/post-merge-validation-surface.md"
          echo "See docs/ops/pmo/github-issue-closeout-protocol.md"

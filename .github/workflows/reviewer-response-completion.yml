name: GATE — Reviewer Response Completion

on:
  pull_request_target:
  pull_request_review:
  workflow_dispatch: {}

jobs:
  reviewer-response-completion:
    runs-on: ubuntu-latest
    steps:
      - name: Status
        run: pwd

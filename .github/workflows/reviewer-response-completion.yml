name: GATE — Reviewer Response Completion

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  reviewer-response-completion:
    runs-on: ubuntu-latest
    steps:
      - name: Transition note
        run: echo "Reviewer response workflow is manual during PR process redesign."

name: GATE — Secret Scan

'on':
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Optional (org accounts): set a free license key as a repo/org secret.
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}
          # Use repo config file.
          GITLEAKS_CONFIG: .gitleaks.toml
          # The required gate is the scanner result. Keep optional SARIF upload
          # from turning an artifact-service outage into a false scan failure.
          GITLEAKS_ENABLE_UPLOAD_ARTIFACT: 'false'

# Skip postinstall scripts that download binaries
# This prevents rclone.js from failing during Cloudflare Pages builds
# The rclone binary is only needed for local development, not for builds
ignore-scripts=true

# Cloudflare Pages / some CI runners may set install-links=false, which prevents node_modules/.bin
# from being created. If .bin is missing, `npm run build` fails with "next: not found".
# Force link creation so the Next.js CLI is available.
install-links=true
bin-links=true

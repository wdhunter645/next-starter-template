Implement PR #<number> exactly as written.

Do NOT use git push.

Use Codex GitHub integration to:
- update the existing PR branch
- ensure commits appear in THIS PR

Do not create a new branch or PR.
Do not leave results only in sandbox.

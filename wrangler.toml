name = "next-starter-template"
main = ".open-next/worker.js"
compatibility_date = "2025-01-01"

# OpenNext Cloudflare adapter configuration
# The adapter generates the worker code and static assets in .open-next/
# This configuration points to the generated worker entry point

[env.dev]
name = "next-starter-template-dev"

const views=[['activePrograms','Active Programs'],['pmoPipeline','PMO Pipeline'],['completedPrograms','Completed Programs']];
const columns=['Program / Project Name','Priority #','Status','% Complete','# of Tasks','# of Tasks Completed','Owner / Agent','Program Description','Anticipated Completion Date'];
const fmt=(value,fallback='TBD')=>value===null||value===undefined||value===''?fallback:String(value);
const esc=(value)=>fmt(value,'').replace(/[&<>"']/g,(char)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const pct=(value)=>typeof value==='number'&&Number.isFinite(value)?`${value}%`:'N/A';
const issueHref=(value)=>{try{const url=new URL(value);return url.protocol==='https:'&&url.hostname==='github.com'?url.href:'#';}catch{return '#';}};
function rowHtml(row){return `<tr><td><a href="${issueHref(row.issueUrl)}">${esc(row.name||'Unnamed PMO item')}</a><br><span class="pill">${esc(row.type||'item')}</span></td><td>${esc(fmt(row.priority))}</td><td>${esc(fmt(row.status))}</td><td>${esc(pct(row.percentComplete))}</td><td>${esc(fmt(row.taskCount,'0'))}</td><td>${esc(fmt(row.tasksCompleted,'0'))}</td><td>${esc(fmt(row.ownerAgent,'Pending Assignment'))}</td><td>${esc(fmt(row.description,''))}</td><td>${esc(fmt(row.anticipatedCompletionDate))}</td></tr>`;}
fetch('dashboard-data.json').then((r)=>r.json()).then((data)=>{document.getElementById('dashboard').innerHTML=`<p class="meta">Generated ${esc(new Date(data.generatedAt).toLocaleString())} from ${esc(data.repository)} GitHub Issues.</p>`+views.map(([key,title])=>{const rows=data.views[key]||[];return `<section><h2>${esc(title)}</h2>${rows.length?`<div class="table-wrap"><table><thead><tr>${columns.map((c)=>`<th>${esc(c)}</th>`).join('')}</tr></thead><tbody>${rows.map(rowHtml).join('')}</tbody></table></div>`:`<p class="empty">No ${esc(title.toLowerCase())} rows are currently available.</p>`}</section>`}).join('');}).catch((error)=>{document.getElementById('dashboard').textContent=`Dashboard data could not be loaded: ${error.message}`;});
